import Checkout from "@/app/components/Checkout";

export const metadata = {
    title: 'Checkout',
    description: 'Checkout page',
}

const Page = () => {
    return <Checkout/>
};

export default Page;